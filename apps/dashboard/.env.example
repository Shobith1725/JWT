# When empty, the dashboard proxies requests through Vite's dev server (recommended for local dev).
# Only set this when the frontend is served separately from the API (e.g., production or tunnel).
VITE_API_URL=
VITE_SOCKET_URL=
